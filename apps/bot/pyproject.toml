[project]
name = "quiz-app-bot"
version = "0.1.0"
description = "Telegram bot for Quiz App notifications"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.10.0",
    "asyncpg>=0.30.0",
    "python-dotenv>=1.0.0",
]
